window.onload =()=>{
    let input  = prompt("Digite quantas pessoas na sala: ");
    let result = apertos(input); 
    alert(`Serão necessários ${result} apertos de mao para ${input} pessoas apertarem as mãoes.`);
}

function apertos(qtdPessoas){
    if(qtdPessoas == 1){
        return 0;
    } else {
        return (apertos(qtdPessoas-1) + (qtdPessoas - 1));
    }
}

function apertos2(qtdPessoas){
    return (qtdPessoas * (qtdPessoas - 1) / 2);
}










































































































































































































































































































































































































































































































































































































































































































































































































































































































